basePath: /
definitions:
  attachments_create.Data:
    properties:
      chall_id:
        type: integer
    required:
    - chall_id
    type: object
  attachments_delete.Data:
    properties:
      chall_id:
        type: integer
      names:
        items:
          type: string
        type: array
    required:
    - chall_id
    - names
    type: object
  categories_create.Data:
    properties:
      name:
        type: string
    required:
    - name
    type: object
  categories_delete.Data:
    properties:
      name:
        type: string
    required:
    - name
    type: object
  categories_update.Data:
    properties:
      name:
        type: string
      new_name:
        type: string
    required:
    - name
    - new_name
    type: object
  challenges_create.Data:
    properties:
      category:
        type: string
      description:
        type: string
      instance_type:
        $ref: '#/definitions/sqlc.InstanceType'
      max_points:
        type: integer
      name:
        type: string
      score_type:
        $ref: '#/definitions/sqlc.ScoreType'
    required:
    - category
    - instance_type
    - max_points
    - name
    - score_type
    type: object
  challenges_delete.Data:
    properties:
      chall_id:
        type: integer
    required:
    - chall_id
    type: object
  challenges_get.Chall:
    properties:
      attachments:
        items:
          type: string
        type: array
      authors:
        items:
          type: string
        type: array
      category:
        type: string
      compose:
        type: string
      conn_type:
        $ref: '#/definitions/sqlc.ConnType'
      description:
        type: string
      envs:
        type: string
      flags:
        items:
          $ref: '#/definitions/sqlc.GetFlagsByChallengeRow'
        type: array
      hash_domain:
        type: boolean
      hidden:
        type: boolean
      host:
        type: string
      image:
        type: string
      instance_type:
        $ref: '#/definitions/sqlc.InstanceType'
      lifetime:
        type: integer
      max_cpu:
        type: string
      max_memory:
        type: integer
      max_points:
        type: integer
      name:
        type: string
      port:
        type: integer
      renewable:
        type: boolean
      score_type:
        $ref: '#/definitions/sqlc.ScoreType'
      solves_list:
        items:
          $ref: '#/definitions/sqlc.GetChallengeSolvesRow'
        type: array
      tags:
        items:
          type: string
        type: array
    type: object
  challenges_get_all.Chall:
    properties:
      attachments:
        items:
          type: string
        type: array
      authors:
        items:
          type: string
        type: array
      category:
        type: string
      conn_type:
        $ref: '#/definitions/sqlc.ConnType'
      description:
        type: string
      first_blood:
        type: boolean
      hash_domain:
        type: boolean
      hidden:
        type: boolean
      host:
        type: string
      id:
        type: integer
      instance:
        type: boolean
      instance_host:
        type: string
      instance_port:
        type: integer
      max_points:
        type: integer
      name:
        type: string
      points:
        type: integer
      port:
        type: integer
      renewable:
        type: boolean
      score_type:
        $ref: '#/definitions/sqlc.ScoreType'
      solved:
        type: boolean
      solves:
        type: integer
      tags:
        items:
          type: string
        type: array
      timeout:
        type: integer
    type: object
  challenges_hidden.Data:
    properties:
      chall_ids:
        items:
          type: integer
        type: array
    required:
    - chall_ids
    type: object
  challenges_update.Data:
    properties:
      authors:
        items:
          type: string
        type: array
      category:
        type: string
      chall_id:
        type: integer
      compose:
        type: string
      conn_type:
        $ref: '#/definitions/sqlc.ConnType'
      description:
        type: string
      envs:
        type: string
      hash_domain:
        type: boolean
      hidden:
        type: boolean
      host:
        type: string
      image:
        type: string
      instance_type:
        $ref: '#/definitions/sqlc.InstanceType'
      lifetime:
        type: integer
      max_cpu:
        type: string
      max_memory:
        type: integer
      max_points:
        type: integer
      name:
        type: string
      port:
        type: integer
      renewable:
        type: boolean
      score_type:
        $ref: '#/definitions/sqlc.ScoreType'
      tags:
        items:
          type: string
        type: array
    required:
    - chall_id
    type: object
  configs_update.Data:
    properties:
      key:
        type: string
      value:
        type: string
    required:
    - key
    - value
    type: object
  flags_create.Data:
    properties:
      chall_id:
        type: integer
      flag:
        type: string
      regex:
        type: boolean
    required:
    - chall_id
    - flag
    type: object
  flags_delete.Data:
    properties:
      chall_id:
        type: integer
      flag:
        type: string
    required:
    - chall_id
    - flag
    type: object
  flags_update.Data:
    properties:
      chall_id:
        type: integer
      flag:
        type: string
      new_flag:
        type: string
      regex:
        type: boolean
    required:
    - chall_id
    - flag
    type: object
  instances_create.Data:
    properties:
      chall_id:
        type: integer
    required:
    - chall_id
    type: object
  instances_delete.Data:
    properties:
      chall_id:
        type: integer
      team_id:
        type: integer
    required:
    - chall_id
    type: object
  instances_update.Data:
    properties:
      chall_id:
        type: integer
    required:
    - chall_id
    type: object
  models.Error:
    properties:
      error:
        type: string
    type: object
  sqlc.Config:
    properties:
      category:
        type: string
      description:
        type: string
      key:
        type: string
      name:
        type: string
      secret:
        type: boolean
      type:
        type: string
      value:
        type: string
    type: object
  sqlc.ConnType:
    enum:
    - TCP
    - HTTP
    - HTTPS
    type: string
    x-enum-varnames:
    - ConnTypeTCP
    - ConnTypeHTTP
    - ConnTypeHTTPS
  sqlc.GetAdminStatsRow:
    properties:
      total_challenges:
        type: integer
      total_correct_submissions:
        type: integer
      total_players:
        type: integer
      total_released_challenges:
        type: integer
      total_submissions:
        type: integer
      total_teams:
        type: integer
      total_users:
        type: integer
    type: object
  sqlc.GetBadgesFromTeamRow:
    properties:
      description:
        type: string
      name:
        type: string
    type: object
  sqlc.GetChallengeSolvesRow:
    properties:
      id:
        type: integer
      name:
        type: string
      timestamp:
        type: string
    type: object
  sqlc.GetFlagsByChallengeRow:
    properties:
      flag:
        type: string
      regex:
        type: boolean
    type: object
  sqlc.GetInstancesRow:
    properties:
      chall_id:
        type: integer
      chall_name:
        type: string
      conn_type:
        $ref: '#/definitions/sqlc.ConnType'
      docker_id:
        type: string
      expires_at:
        type: string
      host:
        type: string
      port:
        type: integer
      team_id:
        type: integer
      team_name:
        type: string
    type: object
  sqlc.GetTeamMembersRow:
    properties:
      id:
        type: integer
      name:
        type: string
      role:
        $ref: '#/definitions/sqlc.UserRole'
      score:
        type: integer
    type: object
  sqlc.GetTotalCategoryChallengesRow:
    properties:
      category:
        type: string
      count:
        type: integer
    type: object
  sqlc.GetUserSolvesRow:
    properties:
      category:
        type: string
      first_blood:
        type: boolean
      id:
        type: integer
      name:
        type: string
      points:
        type: integer
      timestamp:
        type: string
    type: object
  sqlc.InstanceType:
    enum:
    - Static
    - Container
    - Compose
    type: string
    x-enum-varnames:
    - InstanceTypeStatic
    - InstanceTypeContainer
    - InstanceTypeCompose
  sqlc.ScoreType:
    enum:
    - Static
    - Dynamic
    type: string
    x-enum-varnames:
    - ScoreTypeStatic
    - ScoreTypeDynamic
  sqlc.SubmissionStatus:
    enum:
    - Wrong
    - Correct
    - Repeated
    - Invalid
    type: string
    x-enum-varnames:
    - SubmissionStatusWrong
    - SubmissionStatusCorrect
    - SubmissionStatusRepeated
    - SubmissionStatusInvalid
  sqlc.UserRole:
    enum:
    - Player
    - Author
    - Admin
    type: string
    x-enum-varnames:
    - UserRolePlayer
    - UserRoleAuthor
    - UserRoleAdmin
  submissions_create.Data:
    properties:
      chall_id:
        type: integer
      flag:
        type: string
    required:
    - chall_id
    - flag
    type: object
  submissions_create.Response:
    properties:
      first_blood:
        type: boolean
      status:
        $ref: '#/definitions/sqlc.SubmissionStatus'
    type: object
  submissions_delete.Data:
    properties:
      sub_id:
        type: integer
    required:
    - sub_id
    type: object
  submissions_get.Response:
    properties:
      submissions:
        items:
          $ref: '#/definitions/submissions_get.Submissions'
        type: array
      total:
        type: integer
    type: object
  submissions_get.Submissions:
    properties:
      chall_id:
        type: integer
      chall_name:
        type: string
      first_blood:
        type: boolean
      flag:
        type: string
      id:
        type: integer
      status:
        $ref: '#/definitions/sqlc.SubmissionStatus'
      team_id:
        type: integer
      team_name:
        type: string
      timestamp:
        type: string
      user_id:
        type: integer
      user_name:
        type: string
    type: object
  teams_get.Solve:
    properties:
      category:
        type: string
      first_blood:
        type: boolean
      id:
        type: integer
      name:
        type: string
      points:
        type: integer
      timestamp:
        type: string
      user_id:
        type: integer
    type: object
  teams_get.TeamData:
    properties:
      badges:
        items:
          $ref: '#/definitions/sqlc.GetBadgesFromTeamRow'
        type: array
      country:
        type: string
      email:
        type: string
      id:
        type: integer
      members:
        items:
          $ref: '#/definitions/sqlc.GetTeamMembersRow'
        type: array
      name:
        type: string
      role:
        type: string
      score:
        type: integer
      solves:
        items:
          $ref: '#/definitions/teams_get.Solve'
        type: array
      total_category_challenges:
        items:
          $ref: '#/definitions/sqlc.GetTotalCategoryChallengesRow'
        type: array
      user_id:
        type: integer
    type: object
  teams_get_all.Response:
    properties:
      teams:
        items:
          $ref: '#/definitions/teams_get_all.TeamData'
        type: array
      total:
        type: integer
    type: object
  teams_get_all.TeamData:
    properties:
      badges:
        type: object
      country:
        type: string
      id:
        type: integer
      name:
        type: string
      role:
        type: string
      score:
        type: integer
    type: object
  teams_join.Data:
    properties:
      name:
        type: string
      password:
        type: string
    required:
    - name
    - password
    type: object
  teams_password.Data:
    properties:
      new_password:
        type: string
      team_id:
        type: integer
    type: object
  teams_password.Response:
    properties:
      new_password:
        type: string
    type: object
  teams_register.Data:
    properties:
      name:
        type: string
      password:
        type: string
    required:
    - name
    - password
    type: object
  teams_scoreboard.Response:
    properties:
      teams:
        items:
          $ref: '#/definitions/teams_scoreboard.TeamData'
        type: array
      total:
        type: integer
    type: object
  teams_scoreboard.TeamData:
    properties:
      badges:
        type: object
      country:
        type: string
      id:
        type: integer
      name:
        type: string
      score:
        type: integer
    type: object
  teams_scoreboard_graph.Submission:
    properties:
      chall_id:
        type: integer
      first_blood:
        type: boolean
      score:
        type: integer
      timestamp:
        type: string
    type: object
  teams_scoreboard_graph.Top:
    properties:
      submissions:
        items:
          $ref: '#/definitions/teams_scoreboard_graph.Submission'
        type: array
      team_id:
        type: integer
      team_name:
        type: string
    type: object
  teams_search.SearchTeam:
    properties:
      country:
        type: string
      email:
        type: string
      id:
        type: integer
      name:
        type: string
      role:
        $ref: '#/definitions/sqlc.UserRole'
      user_id:
        type: integer
    type: object
  teams_update.Data:
    properties:
      country:
        type: string
      name:
        type: string
    type: object
  users_get.UserData:
    properties:
      country:
        type: string
      email:
        type: string
      id:
        type: integer
      joined_at:
        type: string
      name:
        type: string
      role:
        type: string
      score:
        type: integer
      solves:
        items:
          $ref: '#/definitions/sqlc.GetUserSolvesRow'
        type: array
      team_id:
        type: integer
      total_category_challenges:
        items:
          $ref: '#/definitions/sqlc.GetTotalCategoryChallengesRow'
        type: array
    type: object
  users_get_all.Response:
    properties:
      total:
        type: integer
      users:
        items:
          $ref: '#/definitions/users_get_all.UserData'
        type: array
    type: object
  users_get_all.UserData:
    properties:
      country:
        type: string
      id:
        type: integer
      name:
        type: string
      role:
        type: string
      score:
        type: integer
    type: object
  users_info.Response:
    properties:
      country:
        type: string
      email_verification:
        type: boolean
      end_time:
        type: string
      id:
        type: integer
      name:
        type: string
      role:
        type: string
      start_time:
        type: string
      team_id:
        type: integer
      user_mode:
        type: boolean
    type: object
  users_login.Data:
    properties:
      email:
        type: string
      password:
        type: string
    required:
    - email
    - password
    type: object
  users_password.Data:
    properties:
      new_password:
        type: string
      user_id:
        type: integer
    type: object
  users_password.Response:
    properties:
      new_password:
        type: string
    type: object
  users_register.Data:
    properties:
      email:
        type: string
      name:
        type: string
      password:
        type: string
      token:
        type: string
    required:
    - email
    - name
    - password
    type: object
  users_role.Data:
    properties:
      new_role:
        $ref: '#/definitions/sqlc.UserRole'
      user_id:
        type: integer
    required:
    - new_role
    - user_id
    type: object
  users_search.SearchUser:
    properties:
      country:
        type: string
      email:
        type: string
      id:
        type: integer
      name:
        type: string
      role:
        $ref: '#/definitions/sqlc.UserRole'
    type: object
  users_update.Data:
    properties:
      country:
        type: string
      name:
        type: string
    type: object
info:
  contact: {}
  title: TRXd API
paths:
  /api/attachments:
    delete:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Deletes attachments for an existing challenge with the provided names.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/attachments_delete.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0` | `Names[i] must not exceed 128`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Attachment not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching attachment` | `Error deleting
            attachment`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Deletes attachments for a challenge'
      tags:
      - attachments
    post:
      consumes:
      - multipart/form-data
      description: |-
        Requires **Author** privileges or higher.
        Creates new attachments for an existing challenge with the provided files.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/attachments_create.Data'
      - collectionFormat: csv
        description: the list of files to upload (at least one file is required)
        in: formData
        items:
          type: file
        name: files
        required: true
        type: array
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid multipart form` | `Invalid form
            data` | `Missing required fields` | `ChallID must be at least 0` | `Attachments[i]
            must not exceed 128`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Attachment already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error creating
            attachments`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Creates new attachments for a challenge'
      tags:
      - attachments
  /api/categories:
    delete:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Deletes an existing category with the provided name.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/categories_delete.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Name must not exceed 32`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error deleting category`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Deletes a category'
      tags:
      - categories
    get:
      description: |-
        Requires **Player** privileges or higher (with role **Player** is also required to be in a team and the competition has to be active).
        Retrieves a list of all categories (with role **Player** only visible one are returned).
      produces:
      - application/json
      responses:
        "200":
          description: List of category names
          schema:
            items:
              type: string
            type: array
        "500":
          description: 'Possible errors: `Error fetching categories`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Gets all categories'
      tags:
      - categories
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Updates an existing category with the provided name (also updates its own challenges categories).
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/categories_update.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Name must not exceed 32` | `NewName must not exceed 32`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Category not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Category already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching category` | `Error creating
            category` | `Error updating category` | `Error deleting category`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Updates a category'
      tags:
      - categories
    post:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Creates a new category with the provided name.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/categories_create.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Name must not exceed 32`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Category already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error creating category`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Creates a new category'
      tags:
      - categories
  /api/challenges:
    delete:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Deletes a challenge with the provided ID.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/challenges_delete.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error deleting challenge`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Deletes a challenge'
      tags:
      - challenges
    get:
      description: |-
        Requires **Player** privileges or higher (with role **Player** is also required to be in a team and the competition has to be active).
        Retrieves a list of all challenges (with role **Player** only visible one are returned).
      produces:
      - application/json
      responses:
        "200":
          description: List of challenges with all dysplayable details (except solves)
          schema:
            items:
              $ref: '#/definitions/challenges_get_all.Chall'
            type: array
        "500":
          description: 'Possible errors: `Error fetching challenges`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Gets all challenges'
      tags:
      - challenges
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Updates an existing challenge with the provided fields, only the `chall_id` is required.
      parameters:
      - description: '`chall_id` is required, the rest of the fields are optional,
          only provide the fields you want to update'
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/challenges_update.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `No data provided to update` | `ChallID must be at least 0`
            | `Name must not exceed 128` | `Category must not exceed 32` | `Description
            must not exceed 10240` | `Authors[i] must not exceed 64` | `Tags[i] must
            not exceed 32` | `InstanceType must be one of: Static Container Compose`
            | `MaxPoints must be at least 0` | `ScoreType must be one of: Static Dynamic`
            | `Port must be at least 0` | `Port must not exceed 65535` | `ConnType
            must be one of: TCP HTTP HTTPS` | `Lifetime must be at least 0` | `Invalid
            environment variables` | `MaxMemory must be at least 0` | `Invalid Max
            CPU, must be a positive 32-bit integer`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found` | `Category not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Challenge name already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error updating
            challenge`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Updates details of an existing challenge'
      tags:
      - challenges
    post:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Creates a new challenge with the provided details.
      parameters:
      - description: all fields are required except **description**
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/challenges_create.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Name must not exceed 32` | `Category must not exceed 32` |
            `Description must not exceed 10240` | `InstanceType must be one of: Static
            Container Compose` | `MaxPoints must be at least 0` | `ScoreType must
            be one of: Static Dynamic`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Category not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Challenge already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error creating challenge`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Creates a new challenge'
      tags:
      - challenges
  /api/challenges/:id:
    get:
      description: |-
        Requires **Player** privileges or higher (with role **Player** is also required to be in a team and the competition has to be active).
        Retrieves challenge details (with role **Player** only solves if the challenge is visible).
      parameters:
      - description: Challenge ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: solves is only included if the requester has Player role, otherwise
            all the challenge details are included except the solves
          schema:
            $ref: '#/definitions/challenges_get.Chall'
        "400":
          description: 'Possible errors: `Invalid challenge ID, must be non negative`
            | `id must be at least 0`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Gets challenge details'
      tags:
      - challenges
  /api/challenges/hidden:
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Toggles the hidden status of the specified challenges.
      parameters:
      - description: a list of challenge IDs to toggle hidden status
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/challenges_hidden.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallIDs[i] must be at least 0`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error updating challenge`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Toggles a list of challenges between hidden and visible'
      tags:
      - challenges
  /api/configs:
    get:
      description: |-
        Requires **Admin** privileges.
        Retrieves a list of all configs sorted as defined in the configs file into the backend.
      produces:
      - application/json
      responses:
        "200":
          description: List of configs with all displayable details
          schema:
            items:
              $ref: '#/definitions/sqlc.Config'
            type: array
        "500":
          description: 'Possible errors: `Error fetching configurations`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Admin] Gets all configs'
      tags:
      - configs
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Admin** privileges.
        Updates an existing config's value.
        Note: if `user-mode` is updated, the server will automatically shut down and restart to apply the change.
        Note: if `registry-server` or `registry-username` or `registry-password` are updated, a docker config write will occur, and the auth will be updated.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/configs_update.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Configuration not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error updating configuration`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Admin] Updates details of an existing config'
      tags:
      - configs
  /api/flags:
    delete:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Deletes a flag of a challenge.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/flags_delete.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0` | `Flag must not exceed 256`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error deleting
            flag`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Deletes a flag for a challenge'
      tags:
      - flags
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Updates a flag of a challenge.
      parameters:
      - description: '`chall_id` and `flag` are required, at least one of `regex`
          or `new_flag` must be provided'
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/flags_update.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0` | `Flag must not exceed 256` |
            `NewFlag must not exceed 256`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Flag already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error updating
            flag`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Updates a flag for a challenge'
      tags:
      - flags
    post:
      consumes:
      - application/json
      description: |-
        Requires **Author** privileges or higher.
        Creates a new flag of a challenge.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/flags_create.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0` | `Flag must not exceed 256`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Flag already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error creating
            flag`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Creates a new flag for a challenge'
      tags:
      - flags
  /api/info:
    get:
      description: |-
        Requires no privileges.
        Retrieves various infos about the current user and the CTF.
        It includes email verification status, start and end times if not logged in
        Otherwise it also includes user ID, name, role, user mode, country, and team ID.
      produces:
      - application/json
      responses:
        "200":
          description: the various infos about the current user and the CTF
          schema:
            $ref: '#/definitions/users_info.Response'
        "500":
          description: 'Possible errors: `Error fetching configuration` | `Error fetching
            user`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Gets various infos about the current user and the CTF'
      tags:
      - users
  /api/instances:
    delete:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (with role **Player** is also required to be in a team and the competition has to be active).
        Deletes an instance of a challenge for a team (if **Player**, only visible challenges), with **Admin** role is also possible to delete instances for other teams.
        Note: This endpoint uses docker, so it's critical, remember that all 500 errors will log the full error message.
      parameters:
      - description: '`chall_id` is required, `team_id` is optional and only for admins'
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/instances_delete.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0` | `TeamID must be at least 0` |
            `Challenge is not instanciable`'
          schema:
            $ref: '#/definitions/models.Error'
        "403":
          description: 'Possible errors: `Team not Found`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Already an active instance`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error fetching
            instance` | `Error deleting instance`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Deletes an instance of a challenge'
      tags:
      - instances
    get:
      description: |-
        Requires **Author** privileges or higher.
        Retrieves a list of all instances.
      produces:
      - application/json
      responses:
        "200":
          description: List of instances details
          schema:
            items:
              $ref: '#/definitions/sqlc.GetInstancesRow'
            type: array
        "500":
          description: 'Possible errors: `Error fetching instances`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Gets all instances'
      tags:
      - instances
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (with role **Player** is also required to be in a team and the competition has to be active).
        Updates the lifetime of an existing instance of a challenge for a team (if **Player**, only visible challenges).
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/instances_update.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0` | `Challenge is not instanciable`'
          schema:
            $ref: '#/definitions/models.Error'
        "403":
          description: 'Possible errors: `Team not Found`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found` | `Instance not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Already an active instance`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error fetching
            instance` | `global lifetime is missing` | `Error updating instance`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Updates the lifetime of an existing instance of a challenge'
      tags:
      - instances
    post:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (with role **Player** is also required to be in a team and the competition has to be active).
        Creates a new instance of a challenge for a team (if **Player**, only visible challenges).
        Note: This endpoint uses docker, so it's critical, remember that all 500 errors will log the full error message.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/instances_create.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0` | `Challenge is not instanciable`'
          schema:
            $ref: '#/definitions/models.Error'
        "403":
          description: 'Possible errors: `Team not Found`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Already an active instance`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error fetching
            instance` | `Error creating instance` | `invalid instance: {error message}`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Spawns a new instance of a challenge'
      tags:
      - instances
  /api/login:
    post:
      consumes:
      - application/json
      description: |-
        Requires no privileges.
        Creates a new user session if the credentials are valid and the client is not already logged in.
      parameters:
      - description: the email and password of the user to log in
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/users_login.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Email must not exceed 256` | `Invalid email format` | `Password
            must be at least 8` | `Password must not exceed 64`'
          schema:
            $ref: '#/definitions/models.Error'
        "401":
          description: 'Possible errors: `Invalid email or password`'
          schema:
            $ref: '#/definitions/models.Error'
        "403":
          description: 'Possible errors: `Already logged in`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error logging in` | `Error fetching session`
            | `Error regenerating session` | `Error saving session`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Logs in the current user given the credentials.'
      tags:
      - users
  /api/logout:
    post:
      description: |-
        Requires no privileges.
        Destroys the current user session.
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "500":
          description: 'Possible errors: `Error fetching session` | `Error destroying
            session`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Logs out the current user'
      tags:
      - users
  /api/register:
    post:
      consumes:
      - application/json
      description: |-
        Requires no privileges.
        Creates a new user account if the provided information is valid.
        If the `allow-register` configuration is set to false, registration will be disabled.
        If the `user-mode` configuration is set to true, a new team will also be created for the user.
        If the `mail-enabled` configuration is set to true, a valid JWT token must be provided for registration, it will be sent via the specified Email, then the endpoint will accept the JWT with also the Name and Password.
      parameters:
      - description: the name, email and password of the user to register; or the
          email and then the JWT, name and password
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/users_register.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Email must not exceed 256` | `Invalid email format` | `Name
            must not exceed 64` | `Invalid name` | `Password must be at least 8` |
            `Password must not exceed 64` | `invalid JWT`'
          schema:
            $ref: '#/definitions/models.Error'
        "401":
          description: 'Possible errors: `invalid email` | `invalid token` | `token
            is unverifiable: error while executing keyfunc: invalid signing method`
            | `token is unverifiable: error while executing keyfunc: invalid signing
            algorithm`'
          schema:
            $ref: '#/definitions/models.Error'
        "403":
          description: 'Possible errors: `Registrations are disabled` | `Already registered`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `User already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching configuration` | `Error beginning
            transaction` | `Invalid email expiration` | `Error signing verification
            token` | `Invalid domain` | `Error sending verification email` | `Error
            registering user` | `Error registering team` | `Team already exists` |
            `Error committing transaction` | `Error fetching session` | `Error regenerating
            session` | `Error saving session`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Registers a new user.'
      tags:
      - users
  /api/scoreboard:
    get:
      description: |-
        Requires no privileges.
        Retrieves a list of all teams with score > 0, sorted by score in descending order and by last player submission time.
        Can be paginated by using the `offset` and `limit` query parameters.
      parameters:
      - description: Number of items to skip before starting to collect the result
          set. Default is 0.
        in: query
        name: offset
        type: integer
      - description: Number of items to return. Default is 0, which means no limit.
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 'The total number of teams and the queried teams from the scoreboard.
            Note: the `badges` field is a JSON array of objects, each containing `name`
            and `description` fields as strings.'
          schema:
            $ref: '#/definitions/teams_scoreboard.Response'
        "400":
          description: 'Possible errors: `Invalid parameter`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching scoreboard`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Gets the scoreboard of all teams'
      tags:
      - teams
  /api/scoreboard/graph:
    get:
      description: |-
        Requires no privileges.
        Retrieves a list of the top N teams with score > 0, sorted by score in descending order and by last player submission time, and their submissions history.
      produces:
      - application/json
      responses:
        "200":
          description: The total number of teams and the queried teams from the scoreboard.
          schema:
            items:
              $ref: '#/definitions/teams_scoreboard_graph.Top'
            type: array
        "500":
          description: 'Possible errors: `Error fetching scoreboard graph`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Gets the scoreboard graph data of the top N teams'
      tags:
      - teams
  /api/stats:
    get:
      description: |-
        Requires **Author** privileges or higher.
        Retrieves a list useful stats.
      produces:
      - application/json
      responses:
        "200":
          description: List of stats
          schema:
            $ref: '#/definitions/sqlc.GetAdminStatsRow'
        "500":
          description: 'Possible errors: `Error fetching stats`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Gets stats'
      tags:
      - stats
  /api/submissions:
    delete:
      consumes:
      - application/json
      description: |-
        Requires **Admin** privileges.
        Deletes a submission specified by the submission ID.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/submissions_delete.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `SubID must be at least 0`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error deleting submission`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Admin+] Delete a submission'
      tags:
      - submissions
    get:
      description: |-
        Requires **Author** privileges or higher.
        Retrieves a list of all submissions, can be paginated by using the `offset` and `limit` query parameters.
      parameters:
      - description: Number of items to skip before starting to collect the result
          set. Default is 0.
        in: query
        name: offset
        type: integer
      - description: Number of items to return. Default is 0, which means no limit.
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: List of submissions details
          schema:
            $ref: '#/definitions/submissions_get.Response'
        "400":
          description: 'Possible errors: `Invalid parameter`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching submissions`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Gets all submissions'
      tags:
      - submissions
    post:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (with role **Player** is also required to be in a team and the competition has to be active).
        Creates submission for a challenge for the player's team (if **Player**, only on visible challenges).
        If **Author** role or higher the submission is not created the flag is only validated.
        If the discord webhook is configured and the submission is a first blood, a message will be sent to the discord channel.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/submissions_create.Data'
      produces:
      - application/json
      responses:
        "200":
          description: Submission status and if it's a first blood
          schema:
            $ref: '#/definitions/submissions_create.Response'
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `ChallID must be at least 0` | `Flag must not exceed 256 `'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Challenge not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching challenge` | `Error submitting
            flag`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Create a new submission for a challenge'
      tags:
      - submissions
  /api/teams:
    get:
      description: |-
        Requires no privileges.
        Retrieves a list of all teams, can be paginated by using the `offset` and `limit` query parameters.
      parameters:
      - description: Number of items to skip before starting to collect the result
          set. Default is 0.
        in: query
        name: offset
        type: integer
      - description: Number of items to return. Default is 0, which means no limit.
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 'List of teams. Note: the `badges` field is a JSON array of
            objects, each containing `name` and `description` fields as strings.'
          schema:
            $ref: '#/definitions/teams_get_all.Response'
        "400":
          description: 'Possible errors: `Invalid parameter`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching teams`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Gets all teams'
      tags:
      - teams
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (the user is also required to be in a team).
        Updates the name and/or country of the team the user is currently in.
        If the name is already taken, the request will fail with a `409 Conflict` status code.
      parameters:
      - description: the new team name and/or country
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/teams_update.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Name must not exceed 64` | `Invalid name` | `Invalid country
            code, must be ISO3166-1 alpha-3`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Name already taken`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching team` | `Error beginning
            transaction` | `Error updating team` | `Error committing transaction`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Updates a player own team''s name and/or country'
      tags:
      - teams
  /api/teams/:id:
    get:
      description: |-
        Requires no privileges.
        Retrieves all team details by ID.
      parameters:
      - description: Team ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: List of teams
          schema:
            $ref: '#/definitions/teams_get.TeamData'
        "400":
          description: 'Possible errors: `Invalid team ID, must be non negative` |
            `id must be at least 0`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Team not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching team`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Gets all team details by ID'
      tags:
      - teams
  /api/teams/join:
    get:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher.
        Makes a user join a team given the JWT in the `token` parameter, or it generates the token for the user's team.
      parameters:
      - description: the JWT to join a team
        in: query
        name: token
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `invalid token`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Team not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Already in a team`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching team` | `Error joining team`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Creates a jwt team token / Joins a team with a JWT'
      tags:
      - teams
    post:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (the user is also required to NOT be in a team).
        Makes a user join a team given the credentials.
      parameters:
      - description: all fields are required
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/teams_join.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Name must not exceed 64` | `Password must be at least 8` |
            `Password must not exceed 64`'
          schema:
            $ref: '#/definitions/models.Error'
        "401":
          description: 'Possible errors: `Invalid name or password`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `Team not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Already in a team`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching team` | `Error joining team`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Create a new submission for a challenge'
      tags:
      - teams
  /api/teams/password:
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (the user is also required to be in a team).
        Resets a team's password given the credentials.
        A user can reset their own team's password, while an admin can reset any team's password by providing the team ID in the request body.
        If the new password is not provided, a random password will be generated and returned in the response.
      parameters:
      - description: the team ID to reset the password for (only required if the user
          is an admin and wants to reset the password for a specific team) and the
          new password (if not provided, a random password will be generated)
        in: body
        name: data
        schema:
          $ref: '#/definitions/teams_password.Data'
      produces:
      - application/json
      responses:
        "200":
          description: the new password if it was generated, otherwise an empty response
          schema:
            $ref: '#/definitions/teams_password.Response'
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `TeamID must be at least 0` | `Invalid team ID, must be non
            negative` | `NewPassword must be at least 8` | `NewPassword must not exceed
            64`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error generating random password` | `Error
            resetting team password`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Reset a team''s password'
      tags:
      - teams
  /api/teams/register:
    post:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (the user is also required to be in a team).
        Registers a new team with the provided name and password.
        A user can register a new team, while an admin can register a team on behalf of another user.
        If the name is already taken, the request will fail with a `409 Conflict` status code.
      parameters:
      - description: the team name and password
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/teams_register.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Name must not exceed 64` | `Invalid name` | `Password must
            be at least 8` | `Password must not exceed 64`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Already in a team` | `Team already exists`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching team` | `Error beginning
            transaction` | `Error registering team` | `Error committing transaction`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Register a new team'
      tags:
      - teams
  /api/teams/search:
    get:
      description: |-
        Requires **Author** privileges or higher.
        Retrieves a list of all teams with matching **name** or **email**.
        The search is done through a case-insensitive substring match and trigrams matching.
      parameters:
      - description: The name of the team to search for
        in: query
        name: name
        type: string
      - description: The email of the team to search for
        in: query
        name: email
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: the result of the search, a list of teams
          schema:
            items:
              $ref: '#/definitions/teams_search.SearchTeam'
            type: array
        "400":
          description: 'Possible errors: `Missing required fields` | `Name must not
            exceed 64` | `Invalid name` | `Invalid email format`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching teams`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Gets a list of searched teams'
      tags:
      - teams
  /api/users:
    get:
      description: |-
        Requires no privileges.
        Retrieves a list of all users, can be paginated by using the `offset` and `limit` query parameters.
      parameters:
      - description: Number of items to skip before starting to collect the result
          set. Default is 0.
        in: query
        name: offset
        type: integer
      - description: Number of items to return. Default is 0, which means no limit.
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: List of users
          schema:
            $ref: '#/definitions/users_get_all.Response'
        "400":
          description: 'Possible errors: `Invalid parameter`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching users`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Gets all users'
      tags:
      - users
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher.
        Updates the name and/or country of the user.
        If the name is already taken, the request will fail with a `409 Conflict` status code.
        In single-user mode, the user's team name and/or country will also be updated to match the user's new name and/or country.
      parameters:
      - description: the new user name and/or country
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/users_update.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `Name must not exceed 64` | `Invalid name` | `Invalid country
            code, must be ISO3166-1 alpha-3`'
          schema:
            $ref: '#/definitions/models.Error'
        "409":
          description: 'Possible errors: `Name already taken`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching user` | `Error beginning
            transaction` | `Error updating user` | `Error updating team` | `Error
            committing transaction`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Updates a player own name and/or country'
      tags:
      - users
  /api/users/:id:
    get:
      description: |-
        Requires no privileges.
        Retrieves all user details by ID.
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: List of users
          schema:
            $ref: '#/definitions/users_get.UserData'
        "400":
          description: 'Possible errors: `Invalid user ID, must be non negative` |
            `id must be at least 0`'
          schema:
            $ref: '#/definitions/models.Error'
        "404":
          description: 'Possible errors: `User not found`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching user`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[No Auth] Gets all user details by ID'
      tags:
      - users
  /api/users/password:
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Player** privileges or higher (the user is also required to be in a user).
        Resets a user's password given the credentials.
        A user can reset their own user's password, while an admin can reset any user's password by providing the user ID in the request body.
        If the new password is not provided, a random password will be generated and returned in the response.
      parameters:
      - description: the user ID to reset the password for (only required if the user
          is an admin and wants to reset the password for a specific user) and the
          new password (if not provided, a random password will be generated)
        in: body
        name: data
        schema:
          $ref: '#/definitions/users_password.Data'
      produces:
      - application/json
      responses:
        "200":
          description: the new password if it was generated, otherwise an empty response
          schema:
            $ref: '#/definitions/users_password.Response'
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `UserID must be at least 0` | `Invalid user ID, must be non
            negative` | `NewPassword must be at least 8` | `NewPassword must not exceed
            64`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error generating random password` | `Error
            resetting user password`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Player+] Reset a user''s password'
      tags:
      - users
  /api/users/role:
    patch:
      consumes:
      - application/json
      description: |-
        Requires **Admin** privileges.
        Changes a user's role given the user ID and new role (only **Player** and **Author**).
      parameters:
      - description: the user ID to change the role for and the new role
        in: body
        name: data
        schema:
          $ref: '#/definitions/users_role.Data'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: 'Possible errors: `Invalid JSON format` | `Missing required
            fields` | `UserID must be at least 0` | `NewRole must be one of: Player
            Author Admin` | `Invalid role`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error changing user role`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Admin] Change a user''s role'
      tags:
      - users
  /api/users/search:
    get:
      description: |-
        Requires **Author** privileges or higher.
        Retrieves a list of all users with matching **name** or **email**.
        The search is done through a case-insensitive substring match and trigrams matching.
      parameters:
      - description: The name of the user to search for
        in: query
        name: name
        type: string
      - description: The email of the user to search for
        in: query
        name: email
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: the result of the search, a list of users
          schema:
            items:
              $ref: '#/definitions/users_search.SearchUser'
            type: array
        "400":
          description: 'Possible errors: `Missing required fields` | `Name must not
            exceed 64` | `Invalid name` | `Invalid email format`'
          schema:
            $ref: '#/definitions/models.Error'
        "500":
          description: 'Possible errors: `Error fetching users`'
          schema:
            $ref: '#/definitions/models.Error'
      summary: '[Author+] Gets a list of searched users'
      tags:
      - users
swagger: "2.0"
