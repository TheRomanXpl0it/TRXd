name: TRXd Tests


on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


env:
  PROJECT_NAME: trxd-test-pipeline
  POSTGRES_DB: testdb
  POSTGRES_USER: testuser
  POSTGRES_PASSWORD: testpass
  POSTGRES_PORT: 2345
  REDIS_PORT: 9736
  REDIS_PASSWORD: testpass
  REGISTRY_USERNAME: testuser
  REGISTRY_PASSWORD: '$apr1$SXlYINSe$m0/9UxcCHTpCNuPaCpREq0' # password
  CLICOLOR_FORCE: 1
  TEST_WORKERS: 100
  DISABLE_ANTI_PANIC: 1
  DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}


defaults:
  run:
    working-directory: ./backend


jobs:
  sqlc-diff:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: sqlc-dev/setup-sqlc@v4
      with:
        sqlc-version: '1.30.0'
    - run: sqlc diff


  frontend-tests:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        fetch-depth: 0

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install dependencies
      working-directory: ./frontend
      run: npm ci

    - name: Run unit tests
      working-directory: ./frontend
      run: npm run test:run

    - name: Run svelte-check
      working-directory: ./frontend
      run: npx svelte-check --tsconfig ./tsconfig.json


  backend-tests:
    timeout-minutes: 30
    runs-on: ubuntu-latest
    env:
      POSTGRES_PORT: 5434
      REDIS_PORT: 6381

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Setup trxd-shared-internal
      run: |
        cd ../
        ./net.sh

    - name: Set up Infra
      run: |
        cd ../
        docker compose -p $PROJECT_NAME up -d postgres redis registry traefik

    - name: Build Test Service
      run: |
        cd ../tests/echo-server
        chmod +x ./build.sh
        ./build.sh

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.26'

    - name: Install Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y netcat-openbsd
        sudo apt install -y postgresql-client

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        python3 -m pip install -r ../tests/backend/requirements.txt

    - name: Wait for Postgres
      run: |
        until pg_isready -h localhost -p $POSTGRES_PORT -U testuser -d testdb; do
          echo "Waiting for Postgres..."
          sleep 2
        done

    - name: Build main binary
      run: |
        go build .

    - name: Run Go Unit tests without Redis
      run: |
        REDIS_DISABLE=true go test -v ./...

    - name: Run Go Unit tests
      run: |
        go test -p=1 -v ./...

    - name: Run Python Integration tests
      run: |
        cd ../tests/backend
        chmod +x ./run.sh
        ./run.sh

    - name: Infra Teardown
      run: |
        cd ../
        docker compose -p $PROJECT_NAME down


  backend-lint-check:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.26'

    - name: Install Go tools
      run: |
        go install github.com/gordonklaus/ineffassign@latest
        go install github.com/fzipp/gocyclo/cmd/gocyclo@latest
        go install github.com/client9/misspell/cmd/misspell@latest
        go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest

    - name: Vet
      run: |
        go vet ./...

    - name: Ineffassign
      run: |
        ineffassign ./...

    - name: Gocyclo
      run: |
        gocyclo -ignore "sqlc|_test|test_" -over 15 .

    - name: Misspell
      run: |
        misspell -error .

    - name: GolangCI-Lint
      run: |
        golangci-lint run
